{
  "name": "airtable-formula-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -F shared build && pnpm -F webview build && node scripts/bundle-mcp.mjs && pnpm -F airtable-formula build",
    "dev": "pnpm -F webview dev",
    "package": "pnpm -F airtable-formula package",
    "test": "pnpm -F shared test && pnpm -F webview test && pnpm -F airtable-formula test",
    "packx": "pnpm -F airtable-formula packx",
    "packx:no-bump": "pnpm -F airtable-formula packx:no-bump",
    "version:bump": "pnpm -F airtable-formula version:bump",
    "clean": "rimraf packages/*/dist packages/extension/dist",
    "capture": "node packages/mcp-server/dev-tools/capture.js",
    "capture:cdp": "node packages/mcp-server/dev-tools/capture.js --cdp",
    "capture:mutations": "node packages/mcp-server/dev-tools/capture.js --mutations-only",
    "capture:cdp:mutations": "node packages/mcp-server/dev-tools/capture.js --cdp --mutations-only"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "airtable-user-mcp": "workspace:*",
    "rimraf": "^6.0.1"
  }
}
