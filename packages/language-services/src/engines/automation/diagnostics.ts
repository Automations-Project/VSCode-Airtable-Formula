/**
 * Automation diagnostics engine — stub.
 * Full implementation in plan 04-04.
 */

import type { LsDiagnostic } from '../../types.js';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function automationDiagnostics(_text: string): LsDiagnostic[] {
    return [];
}
