---
phase: 03-script-engine
plan: 04
type: execute
wave: 2
depends_on:
  - "03-02"
files_modified:
  - packages/language-services/src/engines/script/diagnostics.ts
  - packages/language-services/src/engines/script/index.ts
autonomous: true
requirements:
  - SCRIPT-04
  - SCRIPT-05

must_haves:
  truths:
    - "Bare async call without await emits a missing-await Warning diagnostic"
    - "Awaited, returned, assigned, then-chained async calls are NOT flagged"
    - "Unknown bare identifier followed by dot or parens emits unknown-global Warning"
    - "JS built-ins (console, Math, JSON, etc.) are NOT flagged"
    - "Airtable globals (base, table, cursor, etc.) are NOT flagged"
    - "Locally-declared variables and function parameters are NOT flagged"
    - "diagnostics vitest suite passes GREEN"
  artifacts:
    - path: "packages/language-services/src/engines/script/diagnostics.ts"
      provides: "scriptDiagnostics pure function"
      exports: ["scriptDiagnostics"]
    - path: "packages/language-services/src/engines/script/index.ts"
      provides: "complete barrel re-export"
      contains: "export * from './diagnostics.js'"
  key_links:
    - from: "engines/script/diagnostics.ts"
      to: "engines/script/registry.ts"
      via: "import { SCRIPT_GLOBAL_NAMES } from './registry.js'"
      pattern: "SCRIPT_GLOBAL_NAMES"
---

<objective>
Implement scriptDiagnostics() with SCRIPT-04 (missing-await) and SCRIPT-05 (unknown-global) checks.
Two independent text scanners with careful false-positive avoidance.

Purpose: Satisfies SCRIPT-04 and SCRIPT-05. Security: linear regex only (T-03-01), strict allowlist (T-03-02).
Output: diagnostics.ts in engines/script/; index.ts completed with all 4 barrel exports.
</objective>

<execution_context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\03-script-engine\03-CONTEXT.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\03-script-engine\03-RESEARCH.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\03-script-engine\03-PATTERNS.md

<interfaces>
From packages/language-services/src/types.ts:

  LsDiagnostic: { range, message, severity, code?, source? }
  LsSeverity: Error=0, Warning=1, Information=2, Hint=3

Helpers to copy verbatim from formula diagnostics.ts (lines 24-90):
  offsetToPosition, makeRange, getExclusionRanges, isInsideExclusionRange
  (Add template-literal exclusion block to getExclusionRanges after the single-quote block.)

From registry.ts:
  export const SCRIPT_GLOBAL_NAMES: string[];  // 8 global names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create engines/script/diagnostics.ts</name>
  <files>packages/language-services/src/engines/script/diagnostics.ts</files>
  <read_first>
    - packages/language-services/src/engines/formula/diagnostics.ts (first 100 lines: helpers to copy)
    - packages/language-services/src/engines/script/registry.ts (SCRIPT_GLOBAL_NAMES)
    - packages/language-services/src/types.ts (LsDiagnostic, LsSeverity)
    - .planning/phases/03-script-engine/03-RESEARCH.md (Pattern 4: SCRIPT-04; Pattern 5: SCRIPT-05; JS_BUILTIN_ALLOWLIST section; JS_KEYWORDS section)
    - .planning/phases/03-script-engine/03-CONTEXT.md (D-04, D-05, D-06 decisions)
    - .planning/phases/03-script-engine/03-PATTERNS.md (diagnostics.ts pattern assignment section)
    - packages/language-services/src/test/script/diagnostics.test.ts (acceptance contract)
  </read_first>
  <action>
    Write packages/language-services/src/engines/script/diagnostics.ts.

    SECURITY RULE: All regex patterns must be linear. Use character-class repetitions only.
    No nested quantifiers. Reference RESEARCH.md Pattern 4 and Pattern 5 for the exact
    safe patterns to use for the Async-call scanner and the identifier scanner.

    File structure:

    1. Header: "Pure script diagnostics — no vscode imports. SCRIPT-04 missing-await + SCRIPT-05 unknown-global."

    2. Imports: types from ../../types.js; SCRIPT_GLOBAL_NAMES from ./registry.js. No vscode.

    3. Position/exclusion helpers (copy from formula diagnostics.ts lines 24-90 verbatim).
       Add template-literal exclusion to getExclusionRanges using the safe negated-class pattern
       described in PATTERNS.md "Exclusion Ranges Pattern" section.

    4. KNOWN_SAFE: Set built from SCRIPT_GLOBAL_NAMES plus the full JS built-in allowlist
       from RESEARCH.md "JS Built-in Allowlist" section (mandatory minimum + extended set).

    5. JS_KEYWORDS: Set of JS reserved words that appear before ( but are not calls
       (full list in RESEARCH.md "JS Keywords" section).

    6. findStatementStart(text, pos): number
       Scan backwards for statement boundary (semicolon, brace, newline).

    7. checkMissingAwait(text, exclusionRanges): LsDiagnostic[]
       Scan for Async-suffixed calls using the linear pattern from RESEARCH.md Pattern 4.
       Accepted non-flagging patterns (do NOT emit diagnostic):
         - await keyword in statement context
         - return keyword in statement context
         - const/let/var assignment in statement context
         - .then( chain after the call
         - Promise combinator before the call
       Emit Warning diagnostic with code 'missing-await' for non-accepted occurrences.

    8. buildLocalSymbols(text): Set
       Initialize from KNOWN_SAFE. Collect locally-declared identifiers using the linear
       declaration patterns from RESEARCH.md Pattern 5 Phase A:
       const/let/var names, function names, class names, for-of vars, catch vars,
       single arrow params, function parameter lists.

    9. checkUnknownGlobals(text, exclusionRanges): LsDiagnostic[]
       Build local symbol set. Scan for identifiers before dot or paren using the
       linear pattern from RESEARCH.md Pattern 5 Phase B.
       Skip: preceded by dot (method access), in JS_KEYWORDS, in local symbols.
       Emit Warning with code 'unknown-global' for flagged identifiers.

    10. export function scriptDiagnostics(text, _uri?)
        Run both checks, return merged results.
  </action>
  <acceptance_criteria>
    - File exists at packages/language-services/src/engines/script/diagnostics.ts
    - Contains "export function scriptDiagnostics"
    - Contains no import from 'vscode'
    - Contains string "missing-await" as a diagnostic code value
    - Contains string "unknown-global" as a diagnostic code value
    - Contains "JS_KEYWORDS" constant declaration
    - Contains "buildLocalSymbols" function definition
    - Contains "checkMissingAwait" function definition
    - Contains "checkUnknownGlobals" function definition
    - pnpm -F language-services build exits 0
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F language-services build 2>&1 | tail -5</automated>
  </verify>
  <done>diagnostics.ts exists. Build exits 0. No vscode import. All required functions present.</done>
</task>

<task type="auto">
  <name>Task 2: Add diagnostics to index.ts barrel and verify full suite GREEN</name>
  <files>packages/language-services/src/engines/script/index.ts</files>
  <read_first>
    - packages/language-services/src/engines/script/index.ts (current partial barrel from Plan 03-03)
    - packages/language-services/src/test/script/diagnostics.test.ts (all test cases to satisfy)
    - packages/language-services/src/engines/script/diagnostics.ts (just created — verify exports)
  </read_first>
  <action>
    Update index.ts to the complete barrel:
      export * from './registry.js';
      export * from './completions.js';
      export * from './hover.js';
      export * from './diagnostics.js';

    Run vitest. Iterate on diagnostics.ts if any tests fail. Key cases:
    - Chained await must NOT flag: "await base.getTable('X').selectRecordsAsync({})"
    - Local variable must NOT flag: "const myTable = ...; myTable.selectRecordsAsync({})"
    - Function parameter must NOT flag: "function process(record) { return record.id; }"
    - JS built-in must NOT flag: "console.log('hello')"
  </action>
  <acceptance_criteria>
    - index.ts contains all 4 barrel exports (registry, completions, hover, diagnostics)
    - pnpm -F language-services vitest run -t "scriptDiagnostics" exits 0
    - pnpm -F language-services vitest run exits 0 (full suite GREEN)
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F language-services vitest run 2>&1 | tail -20</automated>
  </verify>
  <done>Full vitest suite GREEN. All SCRIPT-04 and SCRIPT-05 test cases pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user document text to scriptDiagnostics() | Regex patterns execute on user-authored script file content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | checkMissingAwait Async-call pattern | mitigate | Regex uses word-boundary (zero-width), simple word-char repetition, literal paren. Linear execution. No nested quantifiers. Exact pattern documented in RESEARCH.md Pattern 4. |
| T-03-01 | Denial of Service | checkUnknownGlobals identifier-before-dot-or-paren | mitigate | Single character-class repetition for identifier. Preceding-dot guard is O(1) array index lookup. Linear execution. Exact pattern in RESEARCH.md Pattern 5. |
| T-03-01 | Denial of Service | template-literal exclusion in getExclusionRanges | mitigate | Negated character class alternated with escape pair — standard safe quoted-string pattern. Linear. No ambiguity between alternatives. |
| T-03-02 | Information Disclosure | unknown-global diagnostic message | mitigate | Message contains only the identifier from user own file. KNOWN_SAFE validated by diagnostics.test.ts comprehensive coverage. |
</threat_model>

<verification>
After both tasks:
- engines/script/ has 5 files
- pnpm -F language-services vitest run exits 0 (full suite GREEN)
- diagnostics.ts has no vscode import
</verification>

<success_criteria>
scriptDiagnostics() implemented. SCRIPT-04 and SCRIPT-05 vitest tests GREEN. All regex patterns are linear. Full engine test suite GREEN.
</success_criteria>

<output>
After completion, create .planning/phases/03-script-engine/03-04-SUMMARY.md
</output>
