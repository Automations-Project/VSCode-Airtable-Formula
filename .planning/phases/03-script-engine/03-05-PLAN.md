---
phase: 03-script-engine
plan: 05
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/extension/syntaxes/airtable-script.tmLanguage.json
  - packages/extension/language-configuration/airtable-script-language-configuration.json
  - packages/extension/icons/script-light.svg
  - packages/extension/icons/script-dark.svg
autonomous: true
requirements:
  - SCRIPT-01
  - SCRIPT-06

must_haves:
  truths:
    - "airtable-script.tmLanguage.json exists and delegates to source.js via include"
    - "airtable-script-language-configuration.json exists with JS comment, bracket, and backtick config"
    - "script-light.svg and script-dark.svg exist as valid SVG placeholder icons"
  artifacts:
    - path: "packages/extension/syntaxes/airtable-script.tmLanguage.json"
      provides: "TextMate grammar delegating to source.js"
      contains: "source.js"
    - path: "packages/extension/language-configuration/airtable-script-language-configuration.json"
      provides: "JS language configuration"
      contains: "lineComment"
    - path: "packages/extension/icons/script-light.svg"
      provides: "Light theme file icon placeholder"
      contains: "svg"
    - path: "packages/extension/icons/script-dark.svg"
      provides: "Dark theme file icon placeholder"
      contains: "svg"
  key_links:
    - from: "package.json contributes.grammars"
      to: "syntaxes/airtable-script.tmLanguage.json"
      via: "path field in grammars contribution"
      pattern: "airtable-script.tmLanguage.json"
    - from: "package.json contributes.languages"
      to: "language-configuration/airtable-script-language-configuration.json"
      via: "configuration field"
      pattern: "airtable-script-language-configuration.json"
---

<objective>
Create all static asset files for the airtable-script language: TextMate grammar, language configuration, and SVG placeholder icons. These have no code dependencies — they can be created in parallel with the engine implementation (Wave 2).

Purpose: SCRIPT-01 (grammar and language config) and SCRIPT-06 (file icons). All four files are static JSON/SVG with no compilation step.
Output: grammar JSON, language config JSON, light SVG icon, dark SVG icon in packages/extension/.
</objective>

<execution_context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\03-script-engine\03-CONTEXT.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\03-script-engine\03-RESEARCH.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\03-script-engine\03-PATTERNS.md

<interfaces>
<!-- Analog files to read before creating new files -->
<!-- packages/extension/syntaxes/airtable-formula.tmLanguage.json (analog — use header shape only, NOT the repository section) -->
<!-- packages/extension/language-configuration/airtable-formula-language-configuration.json (analog — copy structure, apply JS changes) -->
<!-- packages/extension/icons/formula-light.svg (analog — 4-line placeholder SVG) -->

From packages/extension/icons/formula-light.svg (exact content to adapt):
  SVG structure: rect with fill color, text element with letter
  Formula uses fill="#1976D2" (blue) and letter "f"
  Script placeholder: use fill="#388E3C" (green) for light, "#2E7D32" for dark, letter "S"

From packages/extension/syntaxes/airtable-formula.tmLanguage.json (use header shape only):
  Use $schema, scopeName, name, fileTypes, patterns
  CRITICAL: Do NOT copy the formula repository section — script grammar is minimal delegation only

From RESEARCH.md Pattern 6 (exact grammar content):
  patterns: [{ "include": "source.js" }]
  No repository section.

From RESEARCH.md Pattern 7 (package.json grammar entry):
  embeddedLanguages: { "source.airtable-script": "javascript" }
  (Required for bracket matching — not present in formula grammar)

From RESEARCH.md Pattern 8 (language config):
  JS additions over formula config: backtick autoClosingPair, JS indentation rules (brace-based not paren-based)
  Remove folding.markers (formula-specific)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create grammar JSON and language configuration JSON</name>
  <files>
    packages/extension/syntaxes/airtable-script.tmLanguage.json
    packages/extension/language-configuration/airtable-script-language-configuration.json
  </files>
  <read_first>
    - packages/extension/syntaxes/airtable-formula.tmLanguage.json (structural reference — use $schema and header fields only)
    - packages/extension/language-configuration/airtable-formula-language-configuration.json (copy structure, apply JS changes)
    - .planning/phases/03-script-engine/03-RESEARCH.md (Pattern 6: exact grammar content; Pattern 8: exact language config with backtick and indentation changes)
    - .planning/phases/03-script-engine/03-PATTERNS.md (grammar and language config pattern assignments with CRITICAL warnings)
  </read_first>
  <action>
    Create packages/extension/syntaxes/airtable-script.tmLanguage.json.

    Content (from RESEARCH.md Pattern 6 — create from scratch, do NOT copy formula grammar repository):
    {
      "$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
      "scopeName": "source.airtable-script",
      "name": "Airtable Script",
      "fileTypes": ["script", "ats"],
      "patterns": [
        { "include": "source.js" }
      ]
    }

    CRITICAL: No "repository" key. No custom rules. The formula analog's repository section
    is formula-specific and must NOT appear here.

    Create packages/extension/language-configuration/airtable-script-language-configuration.json.

    Base structure from RESEARCH.md Pattern 8 (adapt from formula analog with these changes):
    1. Keep comments block: lineComment "//" and blockComment ["/*", "*/"]
    2. Keep wordPattern: "[a-zA-Z_$][a-zA-Z0-9_$]*" (add $ for JS identifiers)
    3. Replace indentationRules with JS brace-based rules:
         "increaseIndentPattern": "^\\s*[^\\s].*[{(\\[]\\s*$"
         "decreaseIndentPattern": "^\\s*[}\\])]"
    4. Remove the folding.markers block (formula-specific, paren-based)
    5. Keep brackets: [{,}], [[,]], [(,)]
    6. autoClosingPairs: keep all formula pairs, ADD backtick pair:
         { "open": "`", "close": "`", "notIn": ["string", "comment"] }
    7. surroundingPairs: keep all formula pairs, ADD ["`", "`"]
  </action>
  <acceptance_criteria>
    - packages/extension/syntaxes/airtable-script.tmLanguage.json exists
    - Grammar file contains "source.airtable-script" as scopeName
    - Grammar file contains "source.js" as the include pattern
    - Grammar file does NOT contain "repository" key
    - packages/extension/language-configuration/airtable-script-language-configuration.json exists
    - Language config contains "lineComment" with value "//"
    - Language config contains backtick in autoClosingPairs (contains the backtick character)
    - Language config does NOT contain "folding" key with "markers"
    - Both files are valid JSON (parse without error)
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./packages/extension/syntaxes/airtable-script.tmLanguage.json'); require('./packages/extension/language-configuration/airtable-script-language-configuration.json'); console.log('JSON valid')" 2>&1</automated>
  </verify>
  <done>
    Both JSON files exist and parse without error. Grammar delegates to source.js. Language config has backtick pair.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create SVG placeholder icons for script files</name>
  <files>
    packages/extension/icons/script-light.svg
    packages/extension/icons/script-dark.svg
  </files>
  <read_first>
    - packages/extension/icons/formula-light.svg (4-line analog to mirror exactly)
    - packages/extension/icons/formula-dark.svg (analog for dark variant)
    - .planning/phases/03-script-engine/03-PATTERNS.md (script icon pattern assignment: green fill, letter S)
    - .planning/phases/03-script-engine/03-CONTEXT.md (D-14: placeholder SVGs, user swaps when ready)
  </read_first>
  <action>
    Create packages/extension/icons/script-light.svg.
    Model the structure on formula-light.svg (4 lines). Change:
    - fill color: use #388E3C (green) instead of #1976D2 (blue)
    - text content: "S" instead of "f"

    Create packages/extension/icons/script-dark.svg.
    Same structure. Change:
    - fill color: use #2E7D32 (darker green) instead of #1976D2
    - text content: "S"

    These are placeholder icons per D-14 from CONTEXT.md. The user will swap the SVG content
    with final artwork when ready. The icons must be valid SVGs that VS Code can load without
    errors.
  </action>
  <acceptance_criteria>
    - packages/extension/icons/script-light.svg exists
    - packages/extension/icons/script-dark.svg exists
    - script-light.svg contains "svg" namespace and viewBox attribute
    - script-dark.svg contains "svg" namespace and viewBox attribute
    - script-light.svg contains letter "S" as text content
    - script-dark.svg contains letter "S" as text content
    - Files are distinct from formula icon files (different fill color)
  </acceptance_criteria>
  <verify>
    <automated>ls packages/extension/icons/script-light.svg packages/extension/icons/script-dark.svg 2>&1</automated>
  </verify>
  <done>
    Both SVG icon files exist. Each is a valid 4-line SVG with green fill and letter "S". Distinct from formula icons.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package.json contributes to VS Code runtime | Static grammar and config files are loaded by VS Code at extension activation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | airtable-script.tmLanguage.json grammar | accept | Grammar contains only a single include rule delegating to source.js. No custom patterns, no repository. DoS surface is zero — all tokenization is handled by VS Code built-in JavaScript grammar. |
| T-03-02 | Information Disclosure | SVG icon files | accept | Placeholder SVGs contain no user data. Static assets served from extension bundle. No injection surface. |
</threat_model>

<verification>
After both tasks:
- ls packages/extension/syntaxes/airtable-script.tmLanguage.json returns the file
- ls packages/extension/language-configuration/airtable-script-language-configuration.json returns the file
- ls packages/extension/icons/script-light.svg packages/extension/icons/script-dark.svg returns both files
- JSON parse check passes for both JSON files
</verification>

<success_criteria>
All 4 static asset files exist. Grammar correctly delegates to source.js with no repository section. Language config includes JS-appropriate bracket pairs including backtick. SVG icons are valid placeholders.
</success_criteria>

<output>
After completion, create .planning/phases/03-script-engine/03-05-SUMMARY.md
</output>
