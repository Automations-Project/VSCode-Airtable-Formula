---
phase: 06-lsp-server
plan: 05
type: execute
wave: 5
depends_on:
  - "06-03"
  - "06-04"
files_modified:
  - .github/workflows/release.yml
  - packages/lsp-server/README.md
autonomous: true
requirements:
  - LSP-01

must_haves:
  truths:
    - "release.yml includes 'lsp-server' as a target input choice alongside extension and mcp-server"
    - "release.yml has a Bump LSP server version step mirroring the mcp-server bump step"
    - "release.yml has a Publish LSP server to npm step"
    - "release.yml commits packages/lsp-server/package.json on version bump"
    - "release.yml creates a GitHub Release tagged lsp-server/vX.Y.Z"
    - "packages/lsp-server/README.md exists with npx airtable-user-lsp --stdio usage instructions"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "lsp-server release pipeline"
      contains: "lsp-server"
    - path: "packages/lsp-server/README.md"
      provides: "Standalone npm package documentation"
      contains: "npx airtable-user-lsp"
  key_links:
    - from: "release.yml inputs.target"
      to: "lsp-server option"
      via: "options array under target input"
      pattern: "- lsp-server"
    - from: "release.yml Commit version bump step"
      to: "packages/lsp-server/package.json"
      via: "git add packages/lsp-server/package.json"
      pattern: "packages/lsp-server/package.json"
---

<objective>
Add the lsp-server release pipeline target to release.yml and create the standalone README for the airtable-user-lsp npm package.

Purpose: This makes airtable-user-lsp publishable via the existing GitHub Actions release workflow and provides users with the documentation needed to install and configure the LSP server in their editors (LSP-01).

Output:
- release.yml updated with lsp-server target (version bump + npm publish + git tag + GitHub Release)
- packages/lsp-server/README.md with usage docs for npx, Neovim, OpenCode, Zed, Claude Code
</objective>

<execution_context>
@C:\Users\admin\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-lsp-server/06-CONTEXT.md
@.planning/phases/06-lsp-server/06-RESEARCH.md
@.planning/phases/06-lsp-server/06-03-SUMMARY.md

<interfaces>
<!-- release.yml current structure (read from .github/workflows/release.yml) -->
<!-- Key sections to understand before modifying: -->
<!-- - inputs.target options (lines 8-12): extension, mcp-server, both -->
<!-- - Bump MCP server version step (lines 110-149): id: mcp_version, if condition, PKG path, npm view query -->
<!-- - Publish MCP server to npm step (lines 203-210): if condition, working-directory, npm publish -->
<!-- - Commit version bump step (lines 213-241): git add, TARGETS, TAGS variables -->
<!-- - Create MCP server GitHub Release step (lines 257-266): gh release create pattern -->
<!-- - Summary step (lines 269-285): echo lines for mcp-server -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lsp-server target to release.yml</name>
  <files>.github/workflows/release.yml</files>
  <read_first>
    - .github/workflows/release.yml FULL FILE — read completely before making any changes; understand all six sections that need modification
  </read_first>
  <action>
    Read release.yml completely first to understand current structure.

    Make the following changes to release.yml. Each change mirrors the mcp-server pattern exactly, substituting lsp-server specifics:

    CHANGE 1 — Add 'lsp-server' to inputs.target options (after 'mcp-server', before 'both'):
    ```yaml
    options:
      - extension
      - mcp-server
      - lsp-server
      - both
    ```

    CHANGE 2 — Add "Bump LSP server version" step after the "Bump MCP server version" step.
    Mirror the mcp-server bump step exactly, with these substitutions:
    - Step name: "Bump LSP server version"
    - id: lsp_version
    - if: `inputs.target == 'lsp-server'`  (note: NOT 'both' — lsp-server is independent)
    - PKG: "packages/lsp-server/package.json"
    - npm view query: `npm view airtable-user-lsp version 2>/dev/null || echo "0.0.0"`
    - echo: "LSP server: ${BASE} → ${NEXT}"

    ```yaml
    - name: Bump LSP server version
      id: lsp_version
      if: inputs.target == 'lsp-server'
      run: |
        PKG="packages/lsp-server/package.json"
        LOCAL=$(node -p "require('./$PKG').version")

        # Query npm for the latest published version
        PUBLISHED=$(npm view airtable-user-lsp version 2>/dev/null || echo "0.0.0")

        echo "Local: ${LOCAL}, npm: ${PUBLISHED}"

        # Pick the higher version as the base for bumping
        BASE=$(node -e "
          const a = '${LOCAL}'.split('.').map(Number);
          const b = '${PUBLISHED}'.split('.').map(Number);
          const cmp = a[0]-b[0] || a[1]-b[1] || a[2]-b[2];
          console.log(cmp >= 0 ? '${LOCAL}' : '${PUBLISHED}');
        ")

        NEXT=$(node -e "
          const [major, minor, patch] = '${BASE}'.split('.').map(Number);
          const bump = '${{ inputs.bump }}';
          if (bump === 'major') console.log((major+1)+'.0.0');
          else if (bump === 'minor') console.log(major+'.'+(minor+1)+'.0');
          else console.log(major+'.'+minor+'.'+(patch+1));
        ")

        echo "current=${BASE}" >> $GITHUB_OUTPUT
        echo "next=${NEXT}" >> $GITHUB_OUTPUT

        # Write new version to package.json
        node -e "
          const fs = require('fs');
          const p = '$PKG';
          const pkg = JSON.parse(fs.readFileSync(p, 'utf8'));
          pkg.version = '${NEXT}';
          fs.writeFileSync(p, JSON.stringify(pkg, null, 2) + '\n');
        "
        echo "LSP server: ${BASE} → ${NEXT}"
    ```

    CHANGE 3 — Add "Publish LSP server to npm" step after the "Publish MCP server to npm" step:
    ```yaml
    - name: Publish LSP server to npm
      if: |
        !inputs.dry_run &&
        inputs.target == 'lsp-server'
      working-directory: packages/lsp-server
      run: npm publish --provenance --access public
      env:
        NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
    ```

    CHANGE 4 — Add lsp-server block to "Commit version bump and create tags" step.
    After the mcp-server if block, add:
    ```yaml
          if [[ "${{ inputs.target }}" == "lsp-server" ]]; then
            git add packages/lsp-server/package.json
            TARGETS="${TARGETS} lsp-server v${{ steps.lsp_version.outputs.next }}"
            TAGS="${TAGS} lsp-server/v${{ steps.lsp_version.outputs.next }}"
          fi
    ```

    CHANGE 5 — Add "Create LSP server GitHub Release" step after "Create MCP server GitHub Release":
    ```yaml
    - name: Create LSP server GitHub Release
      if: |
        !inputs.dry_run &&
        inputs.target == 'lsp-server'
      run: |
        gh release create "lsp-server/v${{ steps.lsp_version.outputs.next }}" \
          --title "LSP Server v${{ steps.lsp_version.outputs.next }}" \
          --generate-notes \
          --target main
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    ```

    CHANGE 6 — Add lsp-server line to Summary step:
    After the mcp-server echo line, add:
    ```yaml
          if [[ "${{ inputs.target }}" == "lsp-server" ]]; then
            echo "- LSP Server: ${{ steps.lsp_version.outputs.current }} → **${{ steps.lsp_version.outputs.next }}**" >> $GITHUB_STEP_SUMMARY
          fi
    ```
  </action>
  <verify>
    <automated>python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/release.yml').read()); print('release.yml YAML valid')" 2>&amp;1 || node -e "require('js-yaml').load(require('fs').readFileSync('.github/workflows/release.yml','utf8')); console.log('release.yml YAML valid')" 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release.yml contains "- lsp-server" in target options (grep: "^          - lsp-server")
    - .github/workflows/release.yml contains "id: lsp_version" (grep confirms bump step)
    - .github/workflows/release.yml contains "npm view airtable-user-lsp version" (grep confirms)
    - .github/workflows/release.yml contains "working-directory: packages/lsp-server" in publish step (grep confirms)
    - .github/workflows/release.yml contains "lsp-server/v${{ steps.lsp_version.outputs.next }}" (grep confirms tag pattern)
    - .github/workflows/release.yml contains "packages/lsp-server/package.json" in commit step (grep confirms)
    - YAML syntax is valid (python3 yaml.safe_load or node js-yaml parse succeeds)
  </acceptance_criteria>
  <done>release.yml has complete lsp-server target support: version bump + npm publish + commit + tag + GitHub Release</done>
</task>

<task type="auto">
  <name>Task 2: Create packages/lsp-server/README.md (standalone npm package docs)</name>
  <files>packages/lsp-server/README.md</files>
  <read_first>
    - packages/lsp-server/06-CONTEXT.md §D-01 — package includes own README for standalone use
    - packages/lsp-server/06-CONTEXT.md §"Specific Ideas" — cover npx --stdio, editor config examples (Neovim, OpenCode, Zed), TCP attach pattern
    - packages/mcp-server/README.md — structure and tone reference (same project, same user)
  </read_first>
  <action>
    Create packages/lsp-server/README.md with the following content:

    The README must cover:
    1. What it is (brief description)
    2. Quick start (npx airtable-user-lsp --stdio)
    3. Supported file types (.formula/.fx, .ats/.script, .ata/.automation)
    4. Features (diagnostics, completions, hover, signature help for formulas)
    5. Editor configuration examples: Neovim (lspconfig), OpenCode (claude-code.json), Zed (settings.json), Claude Code (settings.json)
    6. Daemon TCP mode section (for advanced users)
    7. Requirements (Node.js >= 20)

    ```markdown
    # airtable-user-lsp

    Airtable language server for formula, script, and automation files.
    Works with any LSP-capable editor — Neovim, Zed, OpenCode, Claude Code, and more.

    ## Features

    - **Diagnostics** — syntax errors and unknown function names in Airtable formulas
    - **Completions** — all Airtable formula functions, scripting globals, and automation APIs
    - **Hover documentation** — function signatures and descriptions on hover
    - **Signature help** — parameter hints while typing formula function arguments
    - Supports `.formula` / `.fx`, `.ats` / `.script`, `.ata` / `.automation` files

    ## Quick Start

    No installation required:

    ```bash
    npx airtable-user-lsp --stdio
    ```

    Or install globally:

    ```bash
    npm install -g airtable-user-lsp
    airtable-user-lsp --stdio
    ```

    **Requirements:** Node.js >= 20

    ## Editor Configuration

    ### Neovim (nvim-lspconfig)

    ```lua
    require('lspconfig').airtable_formula.setup({
      cmd = { 'npx', 'airtable-user-lsp', '--stdio' },
      filetypes = { 'formula', 'airtable-formula', 'airtable-script', 'airtable-automation' },
      root_dir = function(fname)
        return require('lspconfig').util.find_git_ancestor(fname) or vim.fn.getcwd()
      end,
    })
    ```

    ### Zed

    In `.zed/settings.json` or `~/.config/zed/settings.json`:

    ```json
    {
      "lsp": {
        "airtable-formula": {
          "binary": {
            "path": "npx",
            "arguments": ["airtable-user-lsp", "--stdio"]
          }
        }
      }
    }
    ```

    ### OpenCode (Claude Code)

    In `~/.config/claude-code/settings.json` or project `.claude/settings.json`:

    ```json
    {
      "languageServers": {
        "airtable-formula": {
          "command": "npx",
          "args": ["airtable-user-lsp", "--stdio"],
          "filetypes": [".formula", ".fx", ".ats", ".script", ".ata", ".automation"]
        }
      }
    }
    ```

    ### Helix

    In `~/.config/helix/languages.toml`:

    ```toml
    [[language]]
    name = "airtable-formula"
    language-servers = ["airtable-lsp"]

    [language-server.airtable-lsp]
    command = "npx"
    args = ["airtable-user-lsp", "--stdio"]
    ```

    ## Supported File Types

    | Extension | Language ID | Engine |
    |-----------|-------------|--------|
    | `.formula`, `.fx` | `airtable-formula` | Formula engine |
    | `.ats`, `.script` | `airtable-script` | Scripting Extension engine |
    | `.ata`, `.automation` | `airtable-automation` | Automation Script engine |

    ## Daemon TCP Mode (Advanced)

    When the [airtable-user-mcp](https://www.npmjs.com/package/airtable-user-mcp) daemon is running,
    it automatically starts `airtable-user-lsp --tcp` as a subprocess and writes the TCP port to
    `~/.airtable-user-mcp/daemon.lock` as `port_lsp`.

    Multiple editor sessions can share the same LSP server instance by connecting to this TCP port:

    ```bash
    # Read port from lockfile
    PORT=$(node -e "const f=require('fs').readFileSync(require('os').homedir()+'/.airtable-user-mcp/daemon.lock','utf8'); console.log(JSON.parse(f).port_lsp)")
    # Connect your LSP client to 127.0.0.1:$PORT
    ```

    ## License

    MIT
    ```
  </action>
  <verify>
    <automated>node -e "const f = require('fs').readFileSync('packages/lsp-server/README.md','utf8'); console.assert(f.includes('npx airtable-user-lsp'), 'missing npx command'); console.assert(f.includes('## Features'), 'missing Features section'); console.assert(f.includes('port_lsp'), 'missing TCP mode docs'); console.log('README.md OK')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/lsp-server/README.md exists
    - README.md contains "npx airtable-user-lsp --stdio"
    - README.md contains "## Features" section
    - README.md contains "## Editor Configuration" section
    - README.md contains "port_lsp" (daemon TCP mode documented)
    - README.md contains ".formula" (file type table)
    - README.md contains "Node.js >= 20" requirement
  </acceptance_criteria>
  <done>README.md exists with complete standalone documentation for airtable-user-lsp users</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → npm registry | Publishing airtable-user-lsp package to public npm |
| GitHub Actions → GitHub Releases | Creating versioned release artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-01 | Tampering | release.yml — npm publish provenance | mitigate | --provenance flag in npm publish provides SLSA build attestation; provenance: true in package.json publishConfig. Mirrors mcp-server pattern. |
| T-06-05-02 | Repudiation | release.yml — version bump commit | accept | git commit with version bump is signed by github-actions[bot] (configured at workflow start). Matches existing mcp-server pattern — acceptable. |
</threat_model>

<verification>
After Wave 5 completes:

1. `.github/workflows/release.yml` contains "lsp-server" in target options
2. `.github/workflows/release.yml` contains all 6 lsp-server additions (bump, publish, commit, tag, release, summary)
3. YAML is valid (no parse errors)
4. `packages/lsp-server/README.md` exists with all required sections
5. Full phase test suite still green: `pnpm -F airtable-user-lsp test`
</verification>

<success_criteria>
Release pipeline and README complete when:
- release.yml has lsp-server as a selectable target in GitHub Actions UI
- Version bump reads from npm registry (mirrors mcp-server pattern)
- npm publish uses --provenance --access public
- Git tag pattern matches lsp-server/vX.Y.Z
- README has quick start (npx), 4 editor configs, file type table, daemon TCP mode section
</success_criteria>

<output>
After completion, create `.planning/phases/06-lsp-server/06-05-SUMMARY.md`
</output>
