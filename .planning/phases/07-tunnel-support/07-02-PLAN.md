---
phase: 07-tunnel-support
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - packages/mcp-server/src/safe-write.js
  - packages/mcp-server/src/daemon/cloudflared-pins.json
  - packages/mcp-server/src/daemon/tunnel.js
  - packages/mcp-server/src/daemon/install-tunnel.js
autonomous: true
requirements:
  - TUNNEL-01
  - TUNNEL-02

must_haves:
  truths:
    - "safe-write.js exports safeAtomicWriteFileSync that writes to a .tmp file then renames atomically"
    - "cloudflared-pins.json exists in src/daemon/ with version 2026.3.0 and platform-specific SHA-256 hashes"
    - "tunnel.js exports startTunnel() and extractTunnelUrl() with Windows-safe process kill"
    - "install-tunnel.js exports installCloudflared() and getTunnelBinaryPath() using getHomeDir() from paths.js"
  artifacts:
    - path: "packages/mcp-server/src/safe-write.js"
      provides: "Atomic write utility used by tunnel-providers/index.js and cloudflared-named-setup.js"
      exports: ["safeAtomicWriteFileSync"]
    - path: "packages/mcp-server/src/daemon/cloudflared-pins.json"
      provides: "Pinned cloudflared binary checksums for supply chain verification"
      contains: "2026.3.0"
    - path: "packages/mcp-server/src/daemon/tunnel.js"
      provides: "cloudflared subprocess lifecycle + URL extraction"
      exports: ["startTunnel", "extractTunnelUrl"]
    - path: "packages/mcp-server/src/daemon/install-tunnel.js"
      provides: "cloudflared binary download + checksum verification + path resolution"
      exports: ["installCloudflared", "getTunnelBinaryPath"]
  key_links:
    - from: "packages/mcp-server/src/daemon/install-tunnel.js"
      to: "packages/mcp-server/src/paths.js"
      via: "import getHomeDir"
      pattern: "getHomeDir"
    - from: "packages/mcp-server/src/daemon/install-tunnel.js"
      to: "packages/mcp-server/src/daemon/cloudflared-pins.json"
      via: "import assert { type: 'json' }"
      pattern: "cloudflared-pins.json"
    - from: "packages/mcp-server/src/daemon/tunnel-providers/cloudflared-named-setup.js"
      to: "packages/mcp-server/src/safe-write.js"
      via: "import { safeAtomicWriteFileSync } from '../../safe-write.js'"
      pattern: "safe-write.js"
---

<objective>
Port the four foundational tunnel utility files from the Perplexity source into the Airtable mcp-server. These files are prerequisites for all tunnel providers. No server.js modifications in this plan — this plan is pure new files.

Purpose: Establish the safe-write utility, pinned cloudflared checksums, subprocess lifecycle management, and binary installation before providers consume them.
Output: Four new files: safe-write.js, cloudflared-pins.json, tunnel.js, install-tunnel.js
</objective>

<execution_context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\ROADMAP.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\07-tunnel-support\07-CONTEXT.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\07-tunnel-support\07-RESEARCH.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.planning\phases\07-tunnel-support\07-PATTERNS.md

<interfaces>
<!-- From packages/mcp-server/src/paths.js — getHomeDir() is the config dir accessor -->
<!-- Verify by reading the file: should export getHomeDir() returning ~/.airtable-user-mcp -->

<!-- From Perplexity safe-write.js (complete file, 27 lines): -->
```javascript
import { writeFileSync, renameSync, rmSync } from "node:fs";

export function safeAtomicWriteFileSync(path, data, opts) {
  const tmp = `${path}.tmp`;
  try {
    writeFileSync(tmp, data, opts);
    renameSync(tmp, path);
  } catch (err) {
    try { rmSync(tmp, { force: true }); } catch { /* best-effort cleanup */ }
    throw err;
  }
}
```

<!-- From Perplexity tunnel.ts — key adaptation points: -->
<!-- 1. Remove all TypeScript type annotations -->
<!-- 2. Replace "perplexity-user-mcp" with "airtable-user-mcp" in comments/strings -->
<!-- 3. Windows process kill: taskkill /PID <n> /T /F (not SIGTERM) -->
<!-- 4. Scan BOTH stdout and stderr for trycloudflare.com URL -->
<!-- 5. extractTunnelUrl regex: /https:\/\/[a-z0-9-]+\.trycloudflare\.com/iu -->
<!-- 6. ensureNeutralConfig() writes quick-tunnel.yml next to binary, passes --config flag -->

<!-- From Perplexity install-tunnel.ts — key adaptation points: -->
<!-- 1. Replace getConfigDir() import with getHomeDir() from '../paths.js' -->
<!-- 2. Remove TypeScript type annotations -->
<!-- 3. Keep SHA-256 checksum verification logic intact (supply chain security) -->
<!-- 4. Import cloudflared-pins.json with: import pins from "./cloudflared-pins.json" assert { type: "json" } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create safe-write.js and copy cloudflared-pins.json</name>
  <files>packages/mcp-server/src/safe-write.js, packages/mcp-server/src/daemon/cloudflared-pins.json</files>
  <read_first>
    - C:\Users\admin\github-repos\VSCode-Perplexity-MCP\packages\mcp-server\src\safe-write.js (copy verbatim — 27 lines)
    - C:\Users\admin\github-repos\VSCode-Perplexity-MCP\packages\mcp-server\src\daemon\cloudflared-pins.json (copy as-is — pinned checksums for version 2026.3.0)
    - packages/mcp-server/src/daemon/token.js (verify existing write pattern — should show safe-write is not yet used)
  </read_first>
  <action>
**File 1: packages/mcp-server/src/safe-write.js**

Copy the Perplexity safe-write.js verbatim. No adaptations needed. The file is 27 lines:

```javascript
import { writeFileSync, renameSync, rmSync } from "node:fs";

export function safeAtomicWriteFileSync(path, data, opts) {
  const tmp = `${path}.tmp`;
  try {
    writeFileSync(tmp, data, opts);
    renameSync(tmp, path);
  } catch (err) {
    try { rmSync(tmp, { force: true }); } catch { /* best-effort cleanup */ }
    throw err;
  }
}
```

**File 2: packages/mcp-server/src/daemon/cloudflared-pins.json**

Read the Perplexity cloudflared-pins.json and copy it as-is into packages/mcp-server/src/daemon/cloudflared-pins.json. This file contains the pinned version (2026.3.0) and SHA-256 hashes for platform-specific cloudflared binaries. No adaptation needed.

IMPORTANT: Do not modify any SHA-256 hashes or URLs — the checksums must match the actual cloudflared release artifacts. Any change invalidates the supply chain protection.
  </action>
  <verify>
    <automated>node -e "import('./packages/mcp-server/src/safe-write.js').then(m => { console.log(typeof m.safeAtomicWriteFileSync); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })"</automated>
  </verify>
  <acceptance_criteria>
    - packages/mcp-server/src/safe-write.js exists
    - grep -c "safeAtomicWriteFileSync" packages/mcp-server/src/safe-write.js returns at least 1
    - grep -c "renameSync" packages/mcp-server/src/safe-write.js returns 1 (atomic rename present)
    - grep -c ".tmp" packages/mcp-server/src/safe-write.js returns at least 1
    - packages/mcp-server/src/daemon/cloudflared-pins.json exists
    - grep -c "2026.3.0" packages/mcp-server/src/daemon/cloudflared-pins.json returns at least 1
    - grep -c "sha256" packages/mcp-server/src/daemon/cloudflared-pins.json returns at least 1
    - node -e "import('./packages/mcp-server/src/safe-write.js').then(m => console.log(typeof m.safeAtomicWriteFileSync))" outputs "function"
  </acceptance_criteria>
  <done>safe-write.js exports safeAtomicWriteFileSync; cloudflared-pins.json contains 2026.3.0 SHA-256 checksums</done>
</task>

<task type="auto">
  <name>Task 2: Create tunnel.js and install-tunnel.js</name>
  <files>packages/mcp-server/src/daemon/tunnel.js, packages/mcp-server/src/daemon/install-tunnel.js</files>
  <read_first>
    - C:\Users\admin\github-repos\VSCode-Perplexity-MCP\packages\mcp-server\src\daemon\tunnel.ts (read full file — port to JS removing TypeScript)
    - C:\Users\admin\github-repos\VSCode-Perplexity-MCP\packages\mcp-server\src\daemon\install-tunnel.ts (read full file — port to JS)
    - packages/mcp-server/src/paths.js (read to confirm getHomeDir() export signature — replaces getConfigDir() in install-tunnel)
    - .planning/phases/07-tunnel-support/07-PATTERNS.md lines 61–178 (exact adaptation instructions for both files)
    - .planning/phases/07-tunnel-support/07-RESEARCH.md lines 496–507 (don't hand-roll table — must port ensureNeutralConfig, Windows kill, port-drift handling)
  </read_first>
  <action>
**File 1: packages/mcp-server/src/daemon/tunnel.js**

Port from Perplexity tunnel.ts with these adaptations:
1. Remove all TypeScript type annotations (`: string`, `: Promise<...>`, `interface`, `type`, generics)
2. Replace any `"perplexity-user-mcp"` string literals in comments/strings with `"airtable-user-mcp"`
3. In `ensureNeutralConfig()`: the neutral YAML content comment should reference `airtable-user-mcp Quick Tunnel`
4. Keep ALL of these as-is (do NOT simplify):
   - `ensureNeutralConfig()` — writes `quick-tunnel.yml` next to the binary, passes `--config` flag (prevents user's ~/.cloudflared/config.yml hijack — Pitfall 1)
   - `extractTunnelUrl()` with regex `/https:\/\/[a-z0-9-]+\.trycloudflare\.com/iu` scanning both stdout AND stderr
   - Windows-safe process kill via `taskkill /PID <n> /T /F` when `process.platform === 'win32'`
   - `updateState()` and `getState()` pattern for TunnelState tracking
   - `waitUntilReady` Promise that resolves with the URL once the tunnel is ready

Exports required: `startTunnel`, `extractTunnelUrl`
Internal: `ensureNeutralConfig`, `updateState`, `getState`, TunnelState shape

**File 2: packages/mcp-server/src/daemon/install-tunnel.js**

Port from Perplexity install-tunnel.ts with these adaptations:
1. Remove TypeScript type annotations
2. Replace `import { getConfigDir } from '../profiles.js'` with `import { getHomeDir } from '../paths.js'`
3. Replace all usages of `getConfigDir()` with `getHomeDir()` (same semantics — returns ~/.airtable-user-mcp/)
4. Keep ALL of these as-is:
   - `resolvePinnedAssetKey(platform, arch)` — maps process.platform + process.arch to pin key
   - SHA-256 checksum verification: `createHash('sha256').update(archiveBuffer).digest('hex')` vs `asset.sha256` (supply chain protection — Threat T-07-cf-supply-chain)
   - `.tgz` gunzip extraction logic (`gunzipSync` + offset-walking for tar header)
   - `chmodSync(binPath, 0o755)` on POSIX to make binary executable
   - `import pins from "./cloudflared-pins.json" assert { type: "json" }` — JSON import assertion

Import at top: `import { getHomeDir } from '../paths.js';`
Exports required: `installCloudflared`, `getTunnelBinaryPath`, `resolvePinnedAssetKey`

Run `pnpm -F airtable-user-mcp test` after creating both files to confirm the 96 existing tests still pass (new files have no tests yet).
  </action>
  <verify>
    <automated>pnpm -F airtable-user-mcp test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - packages/mcp-server/src/daemon/tunnel.js exists
    - grep -c "startTunnel" packages/mcp-server/src/daemon/tunnel.js returns at least 1 (export present)
    - grep -c "extractTunnelUrl" packages/mcp-server/src/daemon/tunnel.js returns at least 1
    - grep -c "trycloudflare" packages/mcp-server/src/daemon/tunnel.js returns at least 1 (URL regex present)
    - grep -c "taskkill" packages/mcp-server/src/daemon/tunnel.js returns at least 1 (Windows kill present)
    - grep -c "ensureNeutralConfig" packages/mcp-server/src/daemon/tunnel.js returns at least 1 (config hijack prevention)
    - packages/mcp-server/src/daemon/install-tunnel.js exists
    - grep -c "installCloudflared" packages/mcp-server/src/daemon/install-tunnel.js returns at least 1
    - grep -c "getTunnelBinaryPath" packages/mcp-server/src/daemon/install-tunnel.js returns at least 1
    - grep -c "getHomeDir" packages/mcp-server/src/daemon/install-tunnel.js returns at least 1 (NOT getConfigDir)
    - grep -c "sha256" packages/mcp-server/src/daemon/install-tunnel.js returns at least 1 (checksum verification)
    - grep -c "getConfigDir" packages/mcp-server/src/daemon/install-tunnel.js returns 0 (Perplexity path NOT present)
    - pnpm -F airtable-user-mcp test exits 0 (existing 96 tests still pass)
  </acceptance_criteria>
  <done>tunnel.js and install-tunnel.js exist with correct exports; Windows kill + checksum verification + neutral config present; full test suite still green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install-tunnel.js → internet | Downloads cloudflared binary from GitHub releases CDN |
| install-tunnel.js → filesystem | Writes binary to ~/.airtable-user-mcp/bin/ with chmod 755 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | cloudflared binary supply chain | mitigate | SHA-256 checksum verification from cloudflared-pins.json before writing binary to disk; throws on mismatch |
| T-07-04 | Tampering | safe-write.js atomic write | mitigate | write to .tmp first, rename atomically; rmSync .tmp on failure — prevents partial writes corrupting tunnel-settings.json |
| T-07-05 | Information disclosure | tunnel.js stdout/stderr scanning | accept | cloudflared outputs are local process pipes; no sensitive data in URL format scanned |
</threat_model>

<verification>
After completing this plan:
- node -e "import('./packages/mcp-server/src/safe-write.js').then(m => console.log(typeof m.safeAtomicWriteFileSync))" prints "function"
- cloudflared-pins.json contains version 2026.3.0 and SHA-256 hashes
- pnpm -F airtable-user-mcp test exits 0 (96 existing tests pass; new files have no tests yet)
</verification>

<success_criteria>
- safe-write.js: exports safeAtomicWriteFileSync using tmp + rename pattern
- cloudflared-pins.json: version 2026.3.0, SHA-256 hashes per platform
- tunnel.js: exports startTunnel + extractTunnelUrl; Windows kill via taskkill; neutral config; scans both stdout+stderr
- install-tunnel.js: exports installCloudflared + getTunnelBinaryPath; uses getHomeDir(); SHA-256 verification
- Full test suite green (pnpm -F airtable-user-mcp test exits 0)
</success_criteria>

<output>
After completion, create .planning/phases/07-tunnel-support/07-02-SUMMARY.md
</output>
