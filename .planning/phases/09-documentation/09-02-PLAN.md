---
phase: 09-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/mcp-server/README.md
autonomous: true
requirements:
  - DOCS-02

must_haves:
  truths:
    - "packages/mcp-server/README.md has a Transport Modes section describing all three modes"
    - "The three modes are: stdio standalone (AIRTABLE_NO_DAEMON=1), stdio-proxy (default when daemon lock exists), and HTTP daemon direct"
    - "The CLI commands block includes daemon start, daemon stop, and daemon status"
    - "The env vars table includes AIRTABLE_NO_DAEMON"
    - "The Protocol section shows 'stdio + HTTP (StreamableHTTPServerTransport)' not 'stdio (JSON-RPC 2.0)'"
    - "The Quick Start section mentions transparent proxy behaviour and AIRTABLE_NO_DAEMON opt-out"
    - "The Tools heading reads 'Tools (62)' not 'Tools (61)'"
  artifacts:
    - path: "packages/mcp-server/README.md"
      provides: "Transport modes documentation"
      contains: "## Transport Modes"
  key_links:
    - from: "## Transport Modes"
      to: "## Protocol"
      via: "Transport Modes section immediately precedes Protocol section"
      pattern: "Transport Modes"
    - from: "## All CLI commands"
      to: "daemon start"
      via: "daemon subcommands appended to existing CLI block"
      pattern: "daemon start"
---

<objective>
Update packages/mcp-server/README.md with daemon transport modes, --no-daemon opt-out, daemon CLI subcommands, and the corrected tool count.

Purpose: A user reading this README after v2.0 must understand how `npx airtable-user-mcp` behaves differently when the daemon is running, how to opt out, and that there are new `daemon` subcommands. Without this update, users will be confused by silent proxy behaviour and won't know about --no-daemon.
Output: Five targeted edits to packages/mcp-server/README.md — Quick Start update, env vars table row, CLI commands block append, new Transport Modes section (replacing Protocol), and Tools heading fix.
</objective>

<execution_context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-documentation/09-RESEARCH.md
@.planning/phases/09-documentation/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply five targeted edits to packages/mcp-server/README.md</name>
  <files>packages/mcp-server/README.md</files>

  <read_first>
  1. Read packages/mcp-server/README.md fully — confirm current line numbers for the five edit targets:
     - `## Quick Start` section (around line 103)
     - `### Useful environment variables` table (around line 286)
     - `## All CLI commands` block (around line 335)
     - `## Tools (61)` heading (around line 350)
     - `## Protocol` section (around line 518)
  2. Read .planning/phases/09-documentation/09-PATTERNS.md — `packages/mcp-server/README.md` section — for exact text to write.
  3. Read .planning/phases/09-documentation/09-RESEARCH.md — DOCS-02 Gap Analysis section — for verified facts.
  </read_first>

  <action>
  Make the following five edits to packages/mcp-server/README.md. Apply them all in a single edit pass (read the file, plan all insertions, write the updated file).

  **Edit 1 — Quick Start section (around line 103)**

  Replace the paragraph `That's it. Your MCP client connects via **stdio** and gets access to all 62 tools.` with the following three-paragraph block:

  ```markdown
  That's it. Your MCP client connects via **stdio** and gets access to all 62 tools.

  When the daemon is running (started automatically by the VS Code extension, or via `npx airtable-user-mcp daemon start`),
  subsequent `npx airtable-user-mcp` invocations transparently proxy their stdio to the shared daemon —
  so all clients share one Chromium session. To skip the daemon and run in-process: `AIRTABLE_NO_DAEMON=1 npx airtable-user-mcp`.
  ```

  (The first paragraph is unchanged; append the new paragraph immediately after it, separated by one blank line.)

  **Edit 2 — Useful environment variables table (around line 286)**

  Append one new row to the env vars table. The table currently ends with the `AIRTABLE_LOG_LEVEL` row. After that row, add:

  ```markdown
  | `AIRTABLE_NO_DAEMON` | Skip daemon; run in-process stdio directly (backwards-compatible mode) |
  ```

  The table alignment is `|:--|:--|` (left-align both columns) — match the existing rows exactly.

  **Edit 3 — All CLI commands block (around line 335)**

  The current block ends with `npx airtable-user-mcp --help`. Add three lines INSIDE the existing code block, after `--help` and before the closing triple-backtick:

  ```
  npx airtable-user-mcp daemon start     Start the shared background daemon
  npx airtable-user-mcp daemon stop      Stop the running daemon
  npx airtable-user-mcp daemon status    Show daemon status and port (JSON)
  ```

  Use consistent spacing: the command portion is left-aligned, and the description is right-aligned at the same column as the other lines in the block (approximately column 43). Match the spacing style of the existing rows.

  **Edit 4 — Tools heading (around line 350)**

  Change:
  ```
  ## Tools (61)
  ```
  To:
  ```
  ## Tools (62)
  ```

  **Edit 5 — Protocol section (around line 518)**

  Replace the entire `## Protocol` section (from `## Protocol` through the closing `---` separator that follows it — but NOT the `## Find Us` section) with the following:

  ```markdown
  ## Transport Modes

  `airtable-user-mcp` supports three transport modes. The default behaviour is automatic — MCP clients
  do not need to change their configuration when the daemon is present.

  | Mode | How to use | When to use |
  |:-----|:-----------|:------------|
  | **stdio standalone** | `AIRTABLE_NO_DAEMON=1 npx airtable-user-mcp` | Backwards-compatible; one process per client; no daemon |
  | **stdio-proxy** | `npx airtable-user-mcp` (default when daemon lock exists) | Transparent; stdin/stdout bridged to the running daemon |
  | **HTTP (daemon direct)** | `http://127.0.0.1:{port}/mcp` with `Authorization: Bearer {token}` | VS Code extension and other HTTP-capable clients |

  The daemon stores its port and bearer token in `~/.airtable-user-mcp/daemon.lock`.

  ## Protocol

  | | |
  |:--|:--|
  | **Transport** | stdio + HTTP (StreamableHTTPServerTransport) |
  | **MCP Version** | 2025-11-25 |
  | **SDK** | `@modelcontextprotocol/sdk` v1.27.1 |
  ```

  (This replaces the old `## Protocol` section content, inserting the new `## Transport Modes` section before it, and updating the Transport row from `stdio (JSON-RPC 2.0)` to `stdio + HTTP (StreamableHTTPServerTransport)`.)

  **Constraints:**
  - All five edits are independent — do not let one edit affect another's line numbers in the final file.
  - Do not remove or reorder any other section of the README.
  - Table alignment convention: command/variable tables use `|:--|:--|`; the new Transport Modes table uses `|:-----|:-----------|:------------|` (left-align all three columns).
  </action>

  <verify>
    <automated>grep -n "Transport Modes\|AIRTABLE_NO_DAEMON\|daemon start\|Tools (62)\|StreamableHTTPServerTransport" packages/mcp-server/README.md</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "## Transport Modes" packages/mcp-server/README.md` returns `1`
  - `grep -c "AIRTABLE_NO_DAEMON" packages/mcp-server/README.md` returns at least `2` (env vars table row + Transport Modes table cell + Quick Start paragraph)
  - `grep -c "daemon start" packages/mcp-server/README.md` returns `1`
  - `grep -c "daemon stop" packages/mcp-server/README.md` returns `1`
  - `grep -c "daemon status" packages/mcp-server/README.md` returns `1`
  - `grep -c "## Tools (62)" packages/mcp-server/README.md` returns `1`
  - `grep -c "## Tools (61)" packages/mcp-server/README.md` returns `0`
  - `grep -c "StreamableHTTPServerTransport" packages/mcp-server/README.md` returns `1`
  - `grep -c "stdio (JSON-RPC 2.0)" packages/mcp-server/README.md` returns `0`
  - `grep -c "## Protocol" packages/mcp-server/README.md` returns `1` (Protocol section still present, just updated)
  - `grep -c "## Find Us" packages/mcp-server/README.md` returns `1` (Find Us section preserved)
  - `grep -c "stdio-proxy" packages/mcp-server/README.md` returns at least `1`
  - `grep -c "daemon.lock" packages/mcp-server/README.md` returns at least `1`
  </acceptance_criteria>

  <done>packages/mcp-server/README.md describes all three transport modes, lists the three daemon CLI subcommands, adds AIRTABLE_NO_DAEMON to the env vars table, fixes the Tools count to 62, and the Protocol section shows the updated transport string.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation only — no code execution, no auth, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Information Disclosure | packages/mcp-server/README.md | accept | Static markdown file; no secrets, no executable code; no threats |
</threat_model>

<verification>
Read packages/mcp-server/README.md after edit and confirm:
1. `## Transport Modes` section appears before `## Protocol`.
2. The three-row transport modes table is present with correct mode descriptions.
3. The CLI commands block contains all three `daemon` subcommands.
4. The `AIRTABLE_NO_DAEMON` row is in the env vars table.
5. The `## Tools (62)` heading is correct.
6. No other sections were removed or reordered.
</verification>

<success_criteria>
packages/mcp-server/README.md accurately describes v2.0 daemon behaviour: three transport modes, opt-out env var, daemon CLI subcommands, and correct tool count — matching the verified facts in RESEARCH.md.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-02-SUMMARY.md`
</output>
