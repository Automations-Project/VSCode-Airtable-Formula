---
phase: 09-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
autonomous: true
requirements:
  - DOCS-01

must_haves:
  truths:
    - "CHANGELOG.md contains a ## [2.0.0] — Daemon & LSP top-level section"
    - "The v2.0.0 section appears between [Unreleased] and [2.0.11] — not before [Unreleased] and not after [2.0.11]"
    - "The v2.0.0 section contains subsections for Daemon transport, LSP server, Tunnel support, and Setup tab"
    - "Daemon transport subsection lists HTTP MCP server, stdio-proxy mode, opt-out env var, new CLI subcommands, and lockfile fields"
    - "LSP server subsection mentions airtable-user-lsp, standalone --stdio mode, and port_lsp in lockfile"
    - "Tunnel support subsection mentions Cloudflare, ngrok, 401-burst auto-disable, and tunnel URL"
    - "Setup tab subsection mentions MCP snippets (5 IDEs), LSP snippets (4 IDEs), and daemon status block"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v2.0.0 changelog entry"
      contains: "## [2.0.0] — Daemon & LSP"
  key_links:
    - from: "## [Unreleased]"
      to: "## [2.0.0] — Daemon & LSP"
      via: "new section inserted between them"
      pattern: "\\[2\\.0\\.0\\].*Daemon.*LSP"
    - from: "## [2.0.0] — Daemon & LSP"
      to: "## [2.0.11]"
      via: "followed immediately after v2.0.0 section ends"
      pattern: "\\[2\\.0\\.11\\]"
---

<objective>
Add the v2.0.0 milestone changelog entry to CHANGELOG.md.

Purpose: Users and contributors need a single place to understand what changed in v2.0 — daemon transport, LSP server, tunnel support, and Setup tab UI. Without this entry, the release history has a gap between [Unreleased] and [2.0.11].
Output: A new `## [2.0.0] — Daemon & LSP` section in CHANGELOG.md, inserted between the existing `[Unreleased]` block and `## [2.0.11]`.
</objective>

<execution_context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-documentation/09-RESEARCH.md
@.planning/phases/09-documentation/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert ## [2.0.0] — Daemon & LSP section into CHANGELOG.md</name>
  <files>CHANGELOG.md</files>

  <read_first>
  1. Read CHANGELOG.md lines 1–220 to understand the current structure — confirm the exact line where `## [2.0.11]` begins (currently line 217) and confirm `[Unreleased]` is above it.
  2. Read .planning/phases/09-documentation/09-RESEARCH.md — DOCS-01 Gap Analysis section — for the verified facts to include in each subsection.
  3. Read .planning/phases/09-documentation/09-PATTERNS.md — CHANGELOG.md section — for the exact prose to write.
  </read_first>

  <action>
  Insert the following block into CHANGELOG.md between the last line of the `[Unreleased]` block (currently line 215 — the "Build script robustness" bullet) and the `## [2.0.11]` line (currently line 217).

  Add one blank line after line 215, then insert the following content verbatim:

  ```markdown
  ## [2.0.0] — Daemon & LSP

  ### Daemon transport

  `airtable-user-mcp` now runs as a shared background daemon instead of per-client stdio processes.
  One Chromium session is shared across all MCP clients and editor LSP connections.

  - **HTTP MCP server** — `StreamableHTTPServerTransport` on a dynamic port; bearer token auth; SSE events
  - **stdio-proxy mode** — default `npx airtable-user-mcp` transparently bridges stdin/stdout to the daemon when a lock exists
  - **opt-out** — `AIRTABLE_NO_DAEMON=1` (or `--no-daemon`) forces in-process stdio (backwards-compatible)
  - **New CLI subcommands** — `daemon start`, `daemon stop`, `daemon status`
  - **Lockfile** — `~/.airtable-user-mcp/daemon.lock` carries `pid`, `port`, `port_lsp`, `bearerToken`, `tunnelUrl`

  ### LSP server

  New `airtable-user-lsp` npm package — Airtable language server for formula, script, and automation files.

  - Works with any LSP-capable editor (Neovim, Zed, OpenCode, Helix, and more)
  - `npx airtable-user-lsp --stdio` for standalone use; `--tcp` for shared daemon instance
  - Daemon auto-spawns `airtable-user-lsp --tcp` and writes `port_lsp` to the lockfile

  ### Tunnel support

  Cloudflare and ngrok tunnel integration lets remote AI clients reach the local MCP daemon.

  - Two providers: `cloudflared` (Quick Tunnel or Named Tunnel) and `ngrok`
  - Tunnel URL exposed in daemon lockfile and Setup tab
  - 401-burst auto-disable guard

  ### Setup tab

  New Setup tab in the VS Code dashboard with one-click copy snippets for:
  - MCP configuration (5 IDEs × HTTP and stdio transport modes)
  - LSP configuration (4 IDEs × TCP and stdio modes)
  - Daemon status block with health indicators

  mcp-server: 2.4.5. Extension: 2.0.48.

  ```

  Then leave one blank line before `## [2.0.11]`.

  **Insertion rules:**
  - Do NOT modify any existing content above or below the insertion point.
  - Do NOT change the `## [Unreleased]` block or its subentries.
  - Do NOT change the `## [2.0.11]` section or anything below it.
  - The `## [2.0.0]` heading uses an em dash `—` (U+2014), not a hyphen `-`.
  - Each `###` subsection uses **bold-em-dash** bullet style: `- **Term** — prose starting lowercase`.
  - Code/package names are in backticks throughout.
  </action>

  <verify>
    <automated>grep -n "\[2\.0\.0\]" CHANGELOG.md</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "## \[2\.0\.0\] — Daemon & LSP" CHANGELOG.md` returns `1`
  - `grep -c "### Daemon transport" CHANGELOG.md` returns `1`
  - `grep -c "### LSP server" CHANGELOG.md` returns `1`
  - `grep -c "### Tunnel support" CHANGELOG.md` returns `1`
  - `grep -c "### Setup tab" CHANGELOG.md` returns `1`
  - `grep -c "StreamableHTTPServerTransport" CHANGELOG.md` returns `1`
  - `grep -c "AIRTABLE_NO_DAEMON" CHANGELOG.md` returns `1`
  - `grep -c "port_lsp" CHANGELOG.md` returns `1`
  - `grep -c "airtable-user-lsp" CHANGELOG.md` returns `1`
  - `grep -c "401-burst" CHANGELOG.md` returns `1`
  - `grep -c "daemon start" CHANGELOG.md` returns `1`
  - The line containing `## [2.0.0]` must have a LOWER line number than the line containing `## [2.0.11]`
  - The line containing `## [2.0.0]` must have a HIGHER line number than the line containing `## [Unreleased]`
  - `grep -c "## \[Unreleased\]" CHANGELOG.md` returns `1` (the original [Unreleased] block is intact)
  </acceptance_criteria>

  <done>CHANGELOG.md has a new ## [2.0.0] — Daemon & LSP section inserted between [Unreleased] and [2.0.11], with four subsections covering all v2.0 features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation only — no code execution, no auth, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | CHANGELOG.md | accept | Static markdown file; no secrets, no executable code; no threats |
</threat_model>

<verification>
Read CHANGELOG.md after edit and confirm:
1. The file still parses as valid Markdown (no unclosed code blocks from the insertion).
2. The `## [2.0.0]` section sits between `## [Unreleased]` and `## [2.0.11]` in reading order.
3. All four subsections (Daemon transport, LSP server, Tunnel support, Setup tab) are present with correct content.
4. No existing content above or below the insertion was disturbed.
</verification>

<success_criteria>
CHANGELOG.md contains a properly positioned `## [2.0.0] — Daemon & LSP` section with accurate bullet points for all four v2.0 feature groups, matching the Keep a Changelog format used throughout the file.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-01-SUMMARY.md`
</output>
