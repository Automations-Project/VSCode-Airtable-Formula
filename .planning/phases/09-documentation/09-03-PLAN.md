---
phase: 09-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-03

must_haves:
  truths:
    - "README.md has an airtable-user-lsp npm badge in the badge row"
    - "README.md has a ### LSP Server subsection describing airtable-user-lsp"
    - "The LSP Server subsection includes the npx airtable-user-lsp --stdio quickstart command"
    - "The LSP Server subsection mentions daemon TCP mode and port_lsp"
    - "The LSP Server subsection links to packages/lsp-server/README.md for per-editor config"
    - "The monorepo packages table includes a packages/lsp-server row"
    - "The 'What's In This Repo' block says 'three products' not 'two products'"
  artifacts:
    - path: "README.md"
      provides: "LSP server documentation section"
      contains: "### LSP Server"
  key_links:
    - from: "badge row (<p align='center'>)"
      to: "airtable-user-lsp badge"
      via: "new badge appended to existing badge group"
      pattern: "airtable-user-lsp"
    - from: "### LSP Server"
      to: "packages/lsp-server/README.md"
      via: "link in LSP section"
      pattern: "lsp-server/README.md"
---

<objective>
Add the LSP server section to root README.md: npm badge, LSP Server subsection, monorepo table row, and "three products" update.

Purpose: A user who discovers this repo for the first time should immediately know that a standalone LSP server exists. Currently the README says "two products" and has no mention of airtable-user-lsp — users who don't use VS Code won't know the language intelligence is available to them.
Output: Four targeted edits to README.md — new LSP badge in the badge row, new ### LSP Server subsection in the Features section, new row in the monorepo packages table, and "two products" → "three products" update.
</objective>

<execution_context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-documentation/09-RESEARCH.md
@.planning/phases/09-documentation/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply four targeted edits to root README.md</name>
  <files>README.md</files>

  <read_first>
  1. Read README.md fully — confirm current line numbers and exact content for the four edit targets:
     - Badge row `<p align="center">` block (around lines 10–14) — find the closing `</p>` tag
     - "two products" text (around line 115)
     - Features section structure — find where `### MCP Server (62 Tools)` ends and `## Supported IDEs` begins (to locate the LSP section insertion point)
     - Monorepo packages table (around lines 218–226) — find the `packages/mcp-server` row
  2. Read .planning/phases/09-documentation/09-PATTERNS.md — `README.md` section — for exact badge HTML, LSP section prose, table row, and products table update.
  3. Read .planning/phases/09-documentation/09-RESEARCH.md — DOCS-03 Gap Analysis section — for verified LSP facts.
  </read_first>

  <action>
  Make the following four edits to README.md. Apply them all in a single edit pass.

  **Edit 1 — Badge row: add airtable-user-lsp npm badge**

  Locate the `<p align="center">` badge block (lines ~10–14). Find the last `</a>` before `</p>`.
  After the MCP Registry badge line:
  ```html
    <a href="https://registry.modelcontextprotocol.io"><img src="https://img.shields.io/badge/MCP%20Registry-Listed-1D4ED8?style=for-the-badge" alt="MCP Registry listing" /></a>
  ```
  Insert a new line (before `</p>`):
  ```html
    <a href="https://www.npmjs.com/package/airtable-user-lsp"><img src="https://img.shields.io/npm/v/airtable-user-lsp?style=for-the-badge&logo=npm&logoColor=white&label=LSP&color=CB3837" alt="npm airtable-user-lsp version" /></a>
  ```
  The indentation is two spaces (matching existing badge lines). Do not change any existing badge lines.

  **Edit 2 — "What's In This Repo": two products → three products**

  Locate the line (around line 115):
  ```markdown
  This monorepo ships **two products** from one source tree:
  ```
  Change it to:
  ```markdown
  This monorepo ships **three products** from one source tree:
  ```

  Also locate the products table below that line. The table currently has two rows (Airtable Formula and airtable-user-mcp). Add a third row after the `airtable-user-mcp` row:
  ```markdown
  | | **airtable-user-lsp** — Airtable language server | `npx airtable-user-lsp` |
  ```
  Match the existing table row format exactly (the first cell is empty `| |` for rows without an icon image, like the MCP server row). If the MCP server row HAS an `<img>` tag, use the same empty cell format for LSP since there is no LSP icon. Look at the exact existing format before writing.

  **Edit 3 — Add ### LSP Server subsection in the Features section**

  Locate the features section. Find the `### MCP Server` subsection (or `### MCP Server (62 Tools)` — check the exact heading). After the last line of that subsection (before either `---` or `## Supported IDEs`), insert a blank line then:

  ```markdown
  ### LSP Server

  **`airtable-user-lsp`** is a standalone language server for Airtable formula, script, and automation files — works in any LSP-capable editor, not just VS Code.

  ```bash
  # stdio mode — works standalone, no daemon needed
  npx airtable-user-lsp --stdio
  ```

  Features: diagnostics, completions, hover documentation, and signature help for `.formula`, `.ats`, and `.ata` files.

  When the daemon is running, it auto-spawns `airtable-user-lsp --tcp` so multiple editors share one language server instance. The TCP port is written to `~/.airtable-user-mcp/daemon.lock` as `port_lsp`.

  See [`packages/lsp-server/README.md`](packages/lsp-server/README.md) for per-editor configuration (Neovim, Zed, OpenCode, Helix).
  ```

  The code block above uses a bash fence (` ```bash `) for the quickstart command.

  **Edit 4 — Monorepo packages table: add lsp-server row**

  Locate the monorepo packages table (around line 220). The table currently ends with the `packages/mcp-server` row (or a `scripts/` row — check actual order). After the `packages/mcp-server` row and before the `scripts/` row (or at the end if scripts is not present), insert:
  ```markdown
  | `packages/lsp-server` | [`airtable-user-lsp`](https://www.npmjs.com/package/airtable-user-lsp) — LSP server for formula / script / automation files |
  ```
  Match the alignment pattern `|:--------|:------------|` (left-align both columns) used in the existing table.

  **Constraints:**
  - Do NOT create or modify `packages/lsp-server/README.md` — it already exists from Phase 6.
  - Do NOT remove or reorder any existing sections of README.md.
  - The LSP section code block uses ` ```bash ` (not a plain fence).
  - The badge uses `style=for-the-badge` (matching all other badges), `label=LSP`, `color=CB3837` (npm red).
  </action>

  <verify>
    <automated>grep -n "airtable-user-lsp\|three products\|LSP Server\|lsp-server" README.md</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "airtable-user-lsp" README.md` returns at least `4` (badge href, badge img src, LSP section heading/content, monorepo table row)
  - `grep -c "three products" README.md` returns `1`
  - `grep -c "two products" README.md` returns `0`
  - `grep -c "### LSP Server" README.md` returns `1`
  - `grep -c "npx airtable-user-lsp --stdio" README.md` returns `1`
  - `grep -c "port_lsp" README.md` returns `1`
  - `grep -c "packages/lsp-server/README.md" README.md` returns `1`
  - `grep -c "packages/lsp-server" README.md` returns at least `2` (monorepo table + lsp-server/README.md link)
  - `grep -c "label=LSP" README.md` returns `1` (new badge present)
  - `grep -c "## What's In This Repo" README.md` returns `1` (section preserved)
  - `grep -c "packages/mcp-server" README.md` returns at least `1` (existing row preserved)
  </acceptance_criteria>

  <done>README.md has a new LSP Server subsection, an airtable-user-lsp badge, a lsp-server row in the monorepo table, and "three products" in the intro — enabling users of any editor to discover and use the LSP server.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation only — no code execution, no auth, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Information Disclosure | README.md | accept | Static markdown file; no secrets, no executable code; no threats |
</threat_model>

<verification>
Read README.md after edit and confirm:
1. Badge row contains all five badges including the new LSP badge.
2. "three products" table has the airtable-user-lsp row.
3. ### LSP Server subsection appears in the Features section with quickstart command, daemon TCP paragraph, and link to lsp-server/README.md.
4. Monorepo packages table has a `packages/lsp-server` row.
5. No existing sections or content removed.
</verification>

<success_criteria>
README.md surfaces airtable-user-lsp as a first-class product: badge in the header, LSP section in Features, row in the monorepo table, and "three products" in the intro — all cross-referencing packages/lsp-server/README.md for per-editor setup.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-03-SUMMARY.md`
</output>
