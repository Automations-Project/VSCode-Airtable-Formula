---
phase: 09-documentation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
autonomous: true
requirements:
  - DOCS-04

must_haves:
  truths:
    - "CLAUDE.md says 'five packages' not 'four packages' in the Architecture section"
    - "CLAUDE.md has a packages/lsp-server section describing the LSP server"
    - "CLAUDE.md has a daemon subsection under packages/mcp-server describing the src/daemon/ file layout"
    - "The daemon subsection lists port source as C:\\Users\\admin\\github-repos\\VSCode-Perplexity-MCP"
    - "The daemon subsection lists the four ported files (lockfile.ts, launcher.ts, server.ts, attach.ts)"
    - "The daemon subsection lists the Airtable-specific new files (token.js, tunnel.js, etc.)"
    - "The daemon subsection includes the lockfile schema with all eight fields"
    - "The daemon subsection lists the three transport modes"
    - "The packages/mcp-server CLI subcommands line includes daemon start/stop/status"
    - "The PERPLEXITY-MCP-START/END block is preserved and unmodified"
    - "All new content appears ABOVE the PERPLEXITY-MCP-START comment"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Daemon architecture section and lsp-server package description"
      contains: "#### packages/mcp-server — Daemon subsystem"
  key_links:
    - from: "### packages/mcp-server"
      to: "#### packages/mcp-server — Daemon subsystem"
      via: "daemon subsection inserted immediately after the mcp-server section body"
      pattern: "packages/mcp-server.*Daemon subsystem"
    - from: "#### packages/mcp-server — Daemon subsystem"
      to: "### packages/lsp-server"
      via: "lsp-server section follows daemon subsection"
      pattern: "### packages/lsp-server"
    - from: "<!-- PERPLEXITY-MCP-START -->"
      to: "<!-- PERPLEXITY-MCP-END -->"
      via: "block preserved intact, all new content above it"
      pattern: "PERPLEXITY-MCP-START"
---

<objective>
Update CLAUDE.md with the daemon architecture section and lsp-server package description.

Purpose: A developer joining this project or returning after a break needs to understand the new daemon subsystem, which files were ported from the Perplexity project, and that there is now a fifth package (lsp-server). Without these additions, CLAUDE.md is misleading — it says "four packages" and describes mcp-server as stdio-only.
Output: Six targeted edits to CLAUDE.md — "four" → "five" packages, daemon subsection under mcp-server, new lsp-server package section, CLI subcommands line update, Key files list additions, and Build Pipeline note about DaemonManager.
</objective>

<execution_context>
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\admin\github-repos\VSCode-Airtable-Formula\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-documentation/09-RESEARCH.md
@.planning/phases/09-documentation/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply six targeted edits to CLAUDE.md</name>
  <files>CLAUDE.md</files>

  <read_first>
  1. Read CLAUDE.md fully — locate the exact text and line numbers for:
     - Line 41 (or wherever): `**pnpm monorepo** with four packages:` — confirm exact text
     - `### packages/mcp-server` section body (around lines 49–65) — especially the CLI subcommands sentence and the `Key files:` list end
     - `### View config reads — two-call pattern` section (around line 67) — this is the insertion point boundary for the daemon subsection and lsp-server section
     - `## Build Pipeline` section (around line 92) — find the last numbered step and any trailing paragraph
     - The `<!-- PERPLEXITY-MCP-START -->` comment (around line 190) — confirm it exists and is unchanged
  2. Read .planning/phases/09-documentation/09-PATTERNS.md — `CLAUDE.md` section — for the exact text of each subsection to write (daemon subsection content, lsp-server section content, Key files additions, Build Pipeline note).
  3. Read .planning/phases/09-documentation/09-RESEARCH.md — DOCS-04 Gap Analysis section — for verified facts.
  </read_first>

  <action>
  Make the following six edits to CLAUDE.md. Apply them all in a single edit pass.

  **CRITICAL CONSTRAINT:** The `<!-- PERPLEXITY-MCP-START -->` / `<!-- PERPLEXITY-MCP-END -->` block at the bottom of CLAUDE.md (around lines 190–223) MUST NOT be removed, moved, or modified in any way. All new content goes ABOVE the `<!-- PERPLEXITY-MCP-START -->` line.

  ---

  **Edit 1 — Architecture section: "four packages" → "five packages"**

  Locate the line: `**pnpm monorepo** with four packages:`
  Change `four` to `five`.

  Result: `**pnpm monorepo** with five packages:`

  ---

  **Edit 2 — packages/mcp-server CLI subcommands sentence**

  Locate the sentence in the `### packages/mcp-server` section body:
  ```
  The CLI exposes subcommands: `login`, `logout`, `status`, `doctor`, `install-browser`.
  ```
  Change it to:
  ```
  The CLI exposes subcommands: `login`, `logout`, `status`, `doctor`, `install-browser`, `daemon start/stop/status`.
  ```

  ---

  **Edit 3 — packages/mcp-server Key files list: add two entries**

  Locate the `Key files:` bullet list in `### packages/mcp-server`. After the last existing bullet in that list (likely `src/login-runner.js` or `src/health-check.js` — check the actual last line), append two new bullets:
  ```markdown
  - `src/daemon/` — daemon subsystem (see daemon subsection below)
  - `src/safe-write.js` — atomic JSON write helper (used by lockfile, token, and settings)
  ```

  ---

  **Edit 4 — Insert daemon subsection after packages/mcp-server section, before ### View config reads**

  After the `### packages/mcp-server` section body ends (i.e., after the `Key files:` list and its last bullet, but before the `### View config reads` heading), insert a blank line, then the following block verbatim:

  ```markdown
  #### packages/mcp-server — Daemon subsystem

  New in v2.0: `packages/mcp-server/src/daemon/`

  **Port source:** `C:\Users\admin\github-repos\VSCode-Perplexity-MCP`

  Files ported (with Airtable-specific adaptations):
  - `lockfile.ts` → `lockfile.js` — acquire/release/replace/isStale lockfile lifecycle
  - `launcher.ts` → `launcher.js` — `ensureDaemon`, `startDaemon`, `stopDaemon`, `spawnDetachedDaemon`
  - `server.ts` → `server.js` — Express HTTP server (MCP + health + SSE events + tunnel endpoints)
  - `attach.ts` → inlined in `src/index.js` attach-proxy block

  New files (Airtable-specific, not in Perplexity source):
  - `token.js` — bearer token generate/read/rotate
  - `tunnel.js` — tunnel lifecycle coordinator
  - `install-tunnel.js` — cloudflared binary download/verify
  - `safe-write.js` — atomic JSON writes (used by lockfile, token, settings)
  - `cloudflared-pins.json` — SHA256 pin map for cloudflared binary verification
  - `index.js` — barrel export
  - `tunnel-providers/`
    - `types.js` — TunnelProvider interface
    - `cloudflared-quick.js` — Cloudflare Quick Tunnel (ephemeral URL)
    - `cloudflared-named.js` — Cloudflare Named Tunnel (persistent hostname)
    - `cloudflared-named-setup.js` — wizard for named tunnel credential setup
    - `ngrok.js` — ngrok tunnel provider
    - `index.js` — provider registry + settings I/O

  **Lockfile schema** (`~/.airtable-user-mcp/daemon.lock`):

  ```js
  {
    pid: number,
    uuid: string,
    port: number,          // HTTP MCP server port
    port_lsp: number|null, // LSP TCP port (null if lsp-server not spawned)
    bearerToken: string,
    version: string,
    startedAt: string,     // ISO 8601
    tunnelUrl: string|null // Active tunnel URL, null if no tunnel
  }
  ```

  **Transport modes:**
  1. `AIRTABLE_NO_DAEMON=1 npx airtable-user-mcp` — stdio standalone (in-process, no daemon)
  2. `npx airtable-user-mcp` when daemon lock exists — stdio-proxy (stdin/stdout bridged to HTTP daemon)
  3. VS Code extension via `DaemonManager` — direct HTTP to `http://127.0.0.1:{port}/mcp` with bearer token
  ```

  After the daemon subsection block, leave one blank line before `### View config reads`.

  ---

  **Edit 5 — Insert ### packages/lsp-server section before ### View config reads**

  After the daemon subsection (Edit 4) and before `### View config reads`, insert one blank line then the following block verbatim:

  ```markdown
  ### packages/lsp-server
  Airtable language server — **published to npm as `airtable-user-lsp`**. Provides diagnostics, completions, hover, and signature help for Airtable formula, script, and automation files in any LSP-capable editor.

  Entry point: `src/index.ts` (builds to `dist/index.mjs` via tsup).

  Modes:
  - `--stdio` — standalone LSP process (one per editor session)
  - `--tcp` — spawned by daemon; multiple editors share one instance; port written to `port_lsp` in lockfile

  Key files:
  - `src/server.ts` — LSP server init, capability registration
  - `src/tcp-server.ts` — TCP listener for daemon-spawned mode
  - `src/lockfile-writer.ts` — writes `port_lsp` into `~/.airtable-user-mcp/daemon.lock`
  - `src/lsp-convert.ts` — converts internal diagnostics/completions to LSP protocol types
  - `src/router.ts` — routes requests to formula / script / automation engines
  ```

  Leave one blank line after this block and before `### View config reads`.

  ---

  **Edit 6 — Build Pipeline section: add DaemonManager integration note**

  Locate the `## Build Pipeline` section. Find the last numbered step (step 6: `tsup` builds extension to CJS...). After that step and before any trailing content (or end of section), append:

  ```markdown
  **DaemonManager integration:** The extension's `src/mcp/daemon-manager.ts` reads `~/.airtable-user-mcp/daemon.lock` at startup and on file-watch events to check daemon health and expose `port` and `bearerToken` to `src/mcp/registration.ts` for direct HTTP transport.
  ```

  Separate it from step 6 with one blank line.

  ---

  **Post-edit verification:** Before finalising the edit, confirm that:
  - The `<!-- PERPLEXITY-MCP-START -->` comment and everything below it is unchanged.
  - No existing `###` or `##` section headings were removed or reordered.
  </action>

  <verify>
    <automated>grep -n "five packages\|Daemon subsystem\|packages/lsp-server\|daemon start/stop\|PERPLEXITY-MCP-START\|port_lsp\|VSCode-Perplexity-MCP" CLAUDE.md</automated>
  </verify>

  <acceptance_criteria>
  - `grep -c "five packages" CLAUDE.md` returns `1`
  - `grep -c "four packages" CLAUDE.md` returns `0`
  - `grep -c "#### packages/mcp-server — Daemon subsystem" CLAUDE.md` returns `1`
  - `grep -c "### packages/lsp-server" CLAUDE.md` returns `1`
  - `grep -c "VSCode-Perplexity-MCP" CLAUDE.md` returns `1`
  - `grep -c "lockfile.ts.*lockfile.js" CLAUDE.md` returns `1`
  - `grep -c "port_lsp" CLAUDE.md` returns at least `1`
  - `grep -c "daemon start/stop/status" CLAUDE.md` returns `1`
  - `grep -c "airtable-user-lsp" CLAUDE.md` returns at least `1`
  - `grep -c "tcp-server.ts" CLAUDE.md` returns `1`
  - `grep -c "DaemonManager integration" CLAUDE.md` returns `1`
  - `grep -c "PERPLEXITY-MCP-START" CLAUDE.md` returns `1` (block preserved)
  - `grep -c "PERPLEXITY-MCP-END" CLAUDE.md` returns `1` (block preserved)
  - `grep -c "src/daemon/" CLAUDE.md` returns at least `1`
  - Line number of `PERPLEXITY-MCP-START` is GREATER than line number of `### packages/lsp-server` (all new content is above the Perplexity block)
  </acceptance_criteria>

  <done>CLAUDE.md has "five packages", a daemon subsection with full file layout and port source attribution, a packages/lsp-server section, updated CLI subcommands, and a DaemonManager note in Build Pipeline — all above the Perplexity MCP block which is preserved intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Documentation only — no code execution, no auth, no network.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Information Disclosure | CLAUDE.md | accept | Static markdown file; no secrets beyond the port source path (a local dev machine path); no executable code; no threats |
</threat_model>

<verification>
Read CLAUDE.md after edit and confirm:
1. "five packages" appears in the Architecture section header.
2. `#### packages/mcp-server — Daemon subsystem` section is present with all file listings.
3. `### packages/lsp-server` section is present with entry point, modes, and key files.
4. The `<!-- PERPLEXITY-MCP-START -->` block appears at the bottom, unchanged.
5. All new content appears before line containing `<!-- PERPLEXITY-MCP-START -->`.
6. No existing sections were removed.
</verification>

<success_criteria>
CLAUDE.md accurately describes the v2.0 codebase: five-package monorepo, daemon subsystem file layout with Perplexity port attribution, lsp-server package description, and DaemonManager integration note — enabling any developer to onboard without needing to read the full git history.
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation/09-04-SUMMARY.md`
</output>
